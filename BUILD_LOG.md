# Build log

One entry per completed feature. Each entry: feature slug, commit SHA, status, notes.

| # | Feature | Branch | Commit | Status | Notes |
|---|---------|--------|--------|--------|-------|
| a | scaffold | feature/scaffold | 1faa8e3 | done | Next.js 14 + TS + Tailwind v3 + Vitest 1.x + ESLint. 1 smoke test. All gates green (typecheck, lint, test, build). 2 debug cycles: vite dedupe pin (^5.4.21); missing `.eslintrc.json` w/ `next/typescript`. |
| b | project-structure | feature/project-structure | 2006c98 | done | Dir skeletons (components, lib, interfaces, mocks, tests, supabase) + shared types (lib/types.ts) + id helpers (lib/ids.ts) with 8 tests. Gates green. No debug cycles. Advisory: makeRandomId has modest entropy — revisit for per-office URLs in feature i. |
| c | db-schema | feature/db-schema | bfdf1f8 | done | Idempotent supabase/schema.sql: 4 enums, 9 tables, FKs, indexes, RLS enabled everywhere (no policies yet — TODO(auth) markers). 7 shape tests. Gates green. No debug cycles. Note: lib/types.ts reconciliation with SQL enums deferred to later feature. |
| d | interface-layer | feature/interface-layer | cce7561 | done | Port-and-adapter seam for 6 external services (sms/email/storage/maps/ai/auth). Each: `interfaces/<svc>.ts` (interface + `createReal*Service()` stub that throws `NotConfiguredError`) + `mocks/<svc>.ts` (deterministic mock + `reset*Mock()`) + colocated test. `interfaces/index.ts` exports `getServices()` switched by `USE_MOCKS` (unset/"true" → mocks, "false" → real stubs, other → error) and `resetAllMocks()` (hooked into `vitest.setup.ts` via global `beforeEach`). `lib/types.ts` reconciled for the four storage-touching tables (`PickupChannel` +manual, `PickupStatus` narrowed to `pending/assigned/completed/flagged`, `PickupRequest` renamed `notes`→`specialInstructions` + added `sourceIdentifier`/`flaggedReason`/`updatedAt`, `Driver` reshaped to profile-join form). `Route`/`Stop`/`RouteStatus` deferred (flagged in BLOCKERS). 5 entries added to BLOCKERS.md under Unresolved (twilio-sms, inbound-email, supabase, mapbox, anthropic). Postmark chosen for email (SendGrid noted as alt). Real adapters are stubs — zero SDK calls in this feature. |
| f | admin-ui | feature/admin-ui | 8214222 | done | Admin CRUD for drivers/doctors/offices on top of mock storage. New: `lib/slugify.ts` (ASCII-only kebab-case + `ensureUniqueSlug` up to `-99`) with 11 tests; `lib/require-admin.ts` belt-and-suspenders admin gate with 3 tests; `lib/admin-form.ts` shared `AdminFormState` + initial-state constant; `components/AdminLayout.tsx` (server) + `components/AdminNavLink.tsx` (client, active-route highlight). Storage interface gains 9 methods: `getOffice`/`updateOffice`/`getDriver`/`updateDriver`/`listDriverAccounts`/`getDoctor`/`updateDoctor`/`deleteDoctor`/`countAdminDashboard`; `NewDriver` reshaped to `Omit<Driver, "profileId"\|"createdAt"> & { email }` — mock generates `profileId` via `crypto.randomUUID()` and stashes `{email, password: "test1234"}` in a `driverAccounts` side map (with a test-only `getDriverAccount` helper). Real Supabase stub gains matching `NotConfiguredError` stubs. Storage mock: 12 new tests covering all new methods + mixed-state `countAdminDashboard`. `Office` gains `active: boolean` (`lib/types.ts` + schema already had column). Admin tree: `/admin` dashboard (4 count cards, 3 linked), `/admin/drivers` + `/new` + `/[id]`, `/admin/doctors` + `/new` + `/[id]` (Delete uses `window.confirm()` per plan q3), `/admin/offices` + `/new` + `/[id]` (form generates `pickupUrlToken = makeRandomId(12)`, auto-slugifies with `-2..-99` collision suffixes, read-only token + copy-to-clipboard in edit form). Server actions: create/update drivers + deactivate; create/update/delete doctors; create/update/deactivate offices — each with colocated unit tests (16 action tests total) that mock `next/cache.revalidatePath` and `next/navigation.redirect`. Soft-delete policy: drivers and offices toggle `active=false` (no cascade to doctors); doctors hard-delete. Explicitly deferred: component-rendering tests for pages (server-component harness cost > value), E2E, geocoding on office create, admin-editing-admin UI, real `auth.admin.createUser`, cascade on soft-delete office. BLOCKERS [supabase] amended with one sentence re: driver-create flow. All four gates green (typecheck/lint/test/build). |
| g | dispatcher-ui | feature/dispatcher-ui | 9616651 | done | Dispatcher queue, routes, map snapshot, and messages log on top of the mock storage. New: `lib/dates.ts` (`todayIso`, `formatShortDateTime`, `formatDateIsoToShort` — UTC default per open question 7) with 11 tests; `lib/require-dispatcher.ts` accepts dispatcher OR admin role with 4 tests; `components/DispatcherLayout.tsx` + `components/DispatcherNavLink.tsx` (clone of admin chrome). Storage interface: `updatePickupRequestStatus` gains optional `flaggedReason` (cleared when status leaves `"flagged"`), plus 12 new methods — `listRoutes`, `getRoute`, `createRoute`, `updateRouteStatus`, `listStops`, `assignRequestToRoute`, `removeStopFromRoute`, `reorderStops`, `listDriverLocations`, `listMessages`, `createRequestFromMessage`, `countDispatcherDashboard`. Real adapter gets `NotConfiguredError` stubs for each. Types reconciled with schema: `RouteStatus` narrowed to `'pending'|'active'|'completed'`; `Route` to `{id, driverId, routeDate, status, startedAt?, completedAt?, createdAt}` (stops no longer embedded); `Stop` renamed `sequence`→`position`, dropped `officeId`, added `createdAt`; `PickupRequest.officeId` now optional (for message-originated requests); new `DriverLocation` (stringified bigserial id per open question 6) and `Message` types. Storage mock gains four maps/arrays + test-only `seedRoute`/`seedStop`/`seedDriverLocation`/`seedMessage` helpers; `assignRequestToRoute` appends by default, throws on explicit-position collisions (open question 4); `removeStopFromRoute` re-numbers survivors contiguous and reopens the pickup request. Dispatcher tree: `/dispatcher` dashboard (4 linked cards), `/dispatcher/requests` (filter tabs pending/flagged/all, assign-to-route + flag + mark-resolved inline), `/dispatcher/requests/new` manual-request form, `/dispatcher/routes` + `/new` + `/[id]` (up/down reorder with no-op at edges, remove, and three status transition buttons), `/dispatcher/map` (table + callout — becomes a real map when `MAPBOX_TOKEN` lands), `/dispatcher/messages` (all / flagged-only tabs + "convert to request" for orphans). Server actions: 11 total across three `actions.ts` files — each starts with `requireDispatcherSession()` as the first statement and has an "auth bails out before touching storage" test. Storage-mock test suite gains ~25 new cases covering every new method + `updatePickupRequestStatus` flaggedReason behavior + mixed-state `countDispatcherDashboard`. `flagged` filter semantics: orphans OR flagged-linked (open question 8). BLOCKERS: new `[mapbox]` entry for the deferred map; `[supabase]` entry appended with a sub-bullet re: Realtime deferred. Explicitly deferred: component-rendering tests, drag-and-drop reorder, real Supabase adapter, real Mapbox rendering, inbound webhook ingestion, AI re-parsing of flagged messages, per-lab timezone, seed data for messages (feature l will handle). All four gates green (typecheck/lint/test/build). |
| i | pickup-form | feature/pickup-form | 300c802 | done | Public per-office pickup-request form at `/pickup/{slug}-{token}` (no login; `PUBLIC_PATH_PREFIXES` already whitelisted `/pickup/` — verified, no change). New pure helpers: `lib/parse-slug-token.ts` (split on LAST `-`, token `^[a-z0-9]{12}$` matching `makeRandomId(12)`) with 11 tests; `lib/eta.ts` (`estimateEtaText()` v1 placeholder pinning `"within about 2 hours"`) with 1 test; `lib/rate-limit.ts` (`TokenBucket` with injectable `now`, module-scope `pickupFormBucket` at 10/5min per slugToken) with 6 tests. Storage interface + mock: new `findOfficeBySlugToken(slug, token)` that full-scans and only returns `active: true` matches (inactive resolves to null — public form treats deactivated links as unknown). Real Supabase stub gets matching `notConfigured()`. Mock test suite gains 4 cases. New route tree: `app/pickup/[slugToken]/page.tsx` (server component, parses slugToken → `notFound()`; looks up office → `notFound()`), `app/pickup/[slugToken]/_components/PickupRequestForm.tsx` (client, `useFormState`, three-state `idle|error|ok` render with success card), `app/pickup/[slugToken]/actions.ts` (`"use server"`; deliberately NO `requireXSession()` — rate limiter is the only abuse guard; validates notes 10..1000 / urgency ∈ {routine,urgent,stat} defaulting to routine / sampleCount optional integer 1..99; persists as `channel: "web"` with `sourceIdentifier = slugToken`, `specialInstructions = notes`; best-effort email via `getServices().email` when `office.email` present, silently skipped otherwise, try/catch on send so email failures don't fail the request), `app/pickup/not-found.tsx`. 11 action tests: happy (verifies web channel + email content), urgency default to routine, missing/short notes, unknown slugToken (generic banner), office-without-email (no email sent), rate-limit exceeded (exactly 10 persisted on 11 attempts), invalid urgency, invalid sampleCount (`"0"`, `"100"`, `"abc"`). Orchestrator decisions on 4 plan open questions: rate-limit 10/5min (default), no audit logging on inactive-office hit (deferred), generic fallback contact line when `office.phone` absent ("If this is urgent, please contact the lab directly."), English-only copy. `makeRandomId(12)` entropy assessed: 12 base36-ish chars from crypto.getRandomValues, coupled with slug and 10/5min rate limit — sufficient for v1 public URL; no scope expansion. All four gates green (typecheck/lint/test 330/330/build).
| h | driver-ui | feature/driver-ui | 284a381 | done | Driver mobile-web UI on top of the mock storage: today summary, start/complete route, stop cards with "I've arrived" / "Samples picked up" check-ins, single-stop detail page, and a foreground GPS sampler. New: `lib/require-driver.ts` exposes `requireDriverSession` (STRICT driver; every driver server action calls this as its first statement) + `requireDriverOrAdminSession` (driver-or-admin read-only page gate) with 8 tests; `lib/today-route.ts` (`getTodaysRouteForDriver(driverId, timeZone="UTC")`) with 5 tests including a Pacific/Auckland edge case; `lib/office-links.ts` (`googleMapsSearchUrl`) with 3 tests. `components/DriverLayout.tsx` mobile single-column chrome (44px log-out tap target, `max-w-md` centered shell); `app/driver/layout.tsx` adds a `viewport` export with `viewportFit: "cover"` (root `app/layout.tsx` still owns html/body). `components/GpsSampler.tsx` client component — hardcoded 60s tick, `watchPosition` with `enableHighAccuracy: true`/`maximumAge: 30s`/`timeout: 60s`, falls through to "Location unavailable" on permission denial or missing API. No component tests for GpsSampler by design (the load-bearing logic is the server action); a file-top comment documents this. Storage interface: 4 new methods — `getStop`, `markStopArrived` (throws `/already arrived/`), `markStopPickedUp` (enforces arrived→picked-up ordering; throws `/not yet arrived/` or `/already picked up/`), `recordDriverLocation` (appends a row with `NewDriverLocation` input type). Real adapter gets `NotConfiguredError` stubs for each; mock implements them on the existing `state.stops` / `state.driverLocations`; storage-mock test file gains 12 new cases. Schema: added `routes_driver_id_route_date_key unique (driver_id, route_date)` to `public.routes` (per orchestrator decision on plan open question 4); `lib/schema.test.ts` gains an assertion for the constraint name. Driver tree: `/driver` (today summary; admin view accepts `?driverId=`; "Start route" visible only for drivers — admin sees inert hint per plan q5), `/driver/route` (stop list with `<StopCard />` cards, `isCurrent` highlight for the first not-picked-up stop, "Complete route" inline when every stop is done, foreground-only note when `route.status === "active"` per plan q6, `<GpsSampler enabled={status==="active" && role==="driver"} />`), `/driver/route/[stopId]` (detail page with large address, "Open in Maps" deep link, check-in buttons, `notFound()` when a driver tries to view another driver's stop). Server actions (all STRICT): `startRouteAction` + `completeRouteAction` + `recordLocationAction` in `app/driver/actions.ts`; `arriveAtStopAction` + `pickupStopAction` in `app/driver/route/actions.ts`. Every action begins with `requireDriverSession()` and has an auth-bail-out test that spies on storage to confirm no call occurs. `recordLocationAction` validates lat∈[-90,90] lng∈[-180,180] (rejects NaN/±∞/out-of-range) and returns silently (no throw) when the driver has no active route today. `completeRouteAction` re-checks that every stop has `pickedUpAt` before flipping status. Drivers can arrive at any pending stop regardless of position per plan q9 (no server-side strict-order guard). Race on double-tap relies on `useFormStatus` + the storage throw per plan q8 (no extra work). Geolocation note on HTTPS per plan q3: file-top comment in `GpsSampler.tsx`. BLOCKERS: `[mapbox]` entry gets a sub-bullet noting the driver detail page's Google Maps fallback. Explicitly deferred: real Supabase adapter, real Mapbox rendering, background location / PWA, admin-picker UI for driver impersonation (plan q1), `getPickupRequest` interface method (plan q2; detail page scans the list). All four gates green (typecheck/lint/test/build). |
| j | message-inbox | feature/message-inbox | 7164bf6 | done | Inbound SMS + email pipeline on top of mock services. New pure helper `lib/phone.ts` (`normalizeUsPhone` — 10-digit → `+1XXXXXXXXXX`, 11-digit with leading `1` preserved, everything else → null) with 11 tests. New `lib/inbound-rate-limits.ts` exposes module-scope `smsInboundBucket` + `emailInboundBucket` (30/min per normalized `from`) reusing the existing `TokenBucket` class. New `lib/inbound-pipeline.ts` (`handleInboundMessage`) owns all business logic — both the webhook routes and the dispatcher simulate panel call it: canonicalize `from` (SMS via `normalizeUsPhone` with raw fallback, email via trim+lowercase), persist message row first, then look up office via `findOfficeByPhone`/`findOfficeByEmail`. Unknown senders get the "This number/email isn't set up for pickups yet" auto-reply (SKIPPED when SMS `from` failed to normalize per orchestrator decision 1) and `{status: "unknown_sender"}`. Known senders hit `ai.parsePickupMessage`; `confidence < 0.6` → `status: "flagged"`, `flaggedReason: "ai_low_confidence"` + generic ack; else → `status: "pending"` + received-copy auto-reply. Received copy tested both branches: `"for 3 samples"` (known count) and `"for your samples"` (undefined fallback). Inactive-office matches resolve to null → unknown-sender path (orchestrator decision 2, matches the `findOfficeBySlugToken` precedent). Pipeline wraps everything after `createMessage` in try/catch returning `{status: "error", messageId}` — `console.error` logs the throw, no auto-reply side-effect. Storage interface gains 4 methods: `createMessage` (with `NewMessage` input), `findOfficeByPhone` (re-normalizes both sides via `normalizeUsPhone`, filters `active: true`), `findOfficeByEmail` (trim+case-insensitive, filters `active: true`), `linkMessageToRequest` (idempotent on same id, throws `"message already linked"` on different id). Real Supabase adapter gets matching `notConfigured()` stubs. `NewMessage` re-exported through `interfaces/index.ts`. Mock storage test suite gains 4 describe blocks (11 new cases total) covering `createMessage`, `findOfficeByPhone` normalization parity + inactive + unparseable, `findOfficeByEmail` case/whitespace + inactive, `linkMessageToRequest` happy/not-found/already-linked/idempotent. New `lib/inbound-pipeline.test.ts` with 9 cases (unknown SMS / unknown email / unparseable SMS / flagged / received-with-count / received-no-count / email Re:-subject / email no-subject fallback / pipeline-throws-swallows). New HTTP routes: `app/api/sms/inbound/route.ts` (`POST` parses `application/x-www-form-urlencoded`, reads `From`+`Body`, rate-limits on raw `From`, always returns HTTP 200 — `{status: "received"|"flagged"|"unknown_sender"|"error"|"rate_limited"|"invalid_payload"}`, TODO(blockers:twilio) comment for signature verification) and `app/api/email/inbound/route.ts` (JSON body, `From`/`Subject`/`TextBody`-or-`HtmlBody` — v1 mock assumes bare-address `From` per orchestrator decision 3, TODO(blockers:postmark) comment). Both routes: 5–7 route tests each (happy / invalid-payload / rate-limited / pipeline-throws + HtmlBody-fallback + JSON-parse-fail for email). Routes are PUBLIC — no `requireXSession()` call; `/api/` prefix already in `PUBLIC_PATH_PREFIXES` (verified, no edit). The dispatcher simulate panel, by contrast, IS auth-gated server-side: `simulateInboundAction` in `app/dispatcher/messages/actions.ts` calls `requireDispatcherSession()` as the first statement and throws on `USE_MOCKS=false` (orchestrator decision 4 — server-side gate only, no build-time strip). Action validates `channel ∈ {sms, email}`, non-empty `from`, non-empty `body`; invokes `handleInboundMessage` directly (no HTTP hop); `revalidatePath("/dispatcher/messages")` + `"/dispatcher/requests"`. New `SimulateInboundFormState` union + `INITIAL_SIMULATE_INBOUND_STATE`. New client component `app/dispatcher/messages/_components/SimulateInboundPanel.tsx` — two side-by-side forms (SMS/Email) inside an amber-bordered "mock mode only" card, each with its own `useFormState` banner. Mounted in `app/dispatcher/messages/page.tsx` under `process.env.USE_MOCKS !== "false"` (server component evaluates the flag; client bundle does not re-check). `actions.test.ts` extended with 4 new cases under `describe("simulateInboundAction")`: happy SMS (message persisted, both paths revalidated), validation failure (empty `from` → error, no storage side-effect), `USE_MOCKS=false` throws (spy confirms no `createMessage`), auth short-circuit. All four gates green (typecheck/lint/test 379/379/build). No BLOCKERS additions — the two `TODO(blockers:...)` comments point at existing entries. |
| k | business-logic | feature/business-logic | 494d4bd | done | Business-logic glue: 10-min heads-up SMS, route auto-complete, permissions module, ETA-on-assignment, and a `convertRequestToStop` helper — all mock-backed. New: `lib/heads-up.ts` (`maybeNotifyOffice` + `HEADS_UP_THRESHOLD_SECONDS=720` + `HEADS_UP_COPY="Your sample pickup is ~10 minutes away."`; resolves next pending stop via `listStops`, guards on active route / no_next_stop / already_notified / no_office / no_office_coords / no_office_phone / eta_above_threshold; normalizes office phone via `normalizeUsPhone`; marks stop notified even when phone missing to avoid re-compute on every ping; all failure modes RESOLVE with typed `HeadsUpOutcome`, never reject) with 11 tests. New: `lib/permissions.ts` pure functions `canDispatcherEditRoute({role, routeDate, today?})` (dispatcher|admin AND routeDate >= todayIso() — lexicographic string compare) and `canDriverCheckInStop({role, profileId, routeDriverId})` (driver-only + profile match; admins/dispatchers cannot check in) with 12 truth-table tests. New: `lib/request-to-stop.ts` (`convertRequestToStop({routeId, requestId, position?})`) wraps `assignRequestToRoute` + best-effort `maps.etaFor` + `storage.updateStopEta`; ETA branch is try/catch-swallowed so assignment never fails; skips when position <= 1 or either office lacks coords with 6 tests. Storage interface gains 3 methods: `markStopNotified10min(stopId)` (idempotent flag flip, throws on bad id), `updateStopEta(stopId, etaAtIso)` (overwrite, throws on bad id), `getPickupRequest(id)` (single-row lookup, returns null on miss — decided per plan open question 3 as a tiny parallel adjacency). Real Supabase stub gets matching `notConfigured()` entries. Schema: `public.stops` gains `notified_10min boolean not null default false` (between `picked_up_at` and `created_at`); `Stop` type in `lib/types.ts` gains required `notified10min: boolean` with heads-up invariant JSDoc; `assignRequestToRoute` in the mock initializes the flag; `lib/schema.test.ts` gains one assertion. Mock storage test suite: 8 new cases covering `notified10min` init + `markStopNotified10min` (happy/idempotent/bad-id) + `updateStopEta` (happy/bad-id) + `getPickupRequest` (happy/missing). Dispatcher actions (`app/dispatcher/routes/actions.ts`): new private `guardCanEditRoute(routeId, session)` helper called by `addStopToRouteAction`, `removeStopAction`, `moveStopUpAction`, `moveStopDownAction`, `startRouteAction`, `completeRouteAction`, `resetRouteAction` — throws `"cannot edit past route"` when `canDispatcherEditRoute` returns false. `addStopToRouteAction` now calls `convertRequestToStop` instead of storage directly. Every action still has `requireDispatcherSession()` as its first statement. Actions test: 11 new cases (ETA happy + ETA-skip target-no-coords + ETA-skip position-1 + past-date guard for every mutating action); existing tests bumped to `routeDate: "2099-12-31"` to remain robust across run dates. Driver actions: `pickupStopAction` re-lists stops and flips route status to `"completed"` when every `pickedUpAt` is set; isolated in try/catch so auto-complete failure is logged but does not propagate; `loadActiveStopForDriver` now delegates the role/ownership check to `canDriverCheckInStop` for one source of truth. Auto-complete test cases (4 new): last-stop happy-path, multi-stop pending remains, already-picked-up error propagates without calling `updateRouteStatus`, admin session rejected. `recordLocationAction` now calls `maybeNotifyOffice` after `storage.recordDriverLocation` resolves; wrapped in try/catch so heads-up failure never fails the GPS ping. Driver actions test: 5 new heads-up integration cases (near → SMS sent + flag flipped, far → no SMS, already-notified → no SMS, no-phone → no SMS but location persisted, sms.sendSms rejects → location still persisted). Open questions: accepted planner defaults — threshold 12min (q1), mark-notified-on-missing-phone (q2), add `getPickupRequest` parallel (q3), no gate on read-only dispatcher pages (q4), auto-complete in action not storage (q5). All four gates green: typecheck 0 errors, lint 0 issues, vitest 436/436 (was 379), next build succeeds. |
| e | auth-skeleton | feature/auth-skeleton | 7cc17f0 | done | Login + route protection on top of the `auth` interface. New: `lib/session.ts` (mock-grade `ld_session` base64-JSON cookie, pure `encodeSession`/`decodeSession` Edge-safe + `getSession`/`setSession`/`clearSession` via `next/headers`) + 12 codec tests; `lib/auth-rules.ts` pure `evaluateAccess({pathname, role})` (public-path allowlist, exact-or-slash prefix matching so `/driverhack` doesn't collide with `/driver`, admin sees all trees, driver/dispatcher bounced back to own tree, unauthenticated → `/login?next=<encoded>`) + 49 truth-table tests; Edge `middleware.ts` consumes `decodeSession` + `SESSION_COOKIE` from `lib/session` (import-first strategy held; no inline fallback needed) with a matcher excluding `_next` + static files; `app/login/page.tsx` client form using `useFormState`, mock-credential hint block; `app/login/actions.ts` server action validates empty input, calls `getServices().auth.signIn`, sets session, and redirects to `next` only if `evaluateAccess` allows it for that role (open-redirect guard: must start with `/` and not `//`); `app/logout/route.ts` GET+POST → 303 to `/login`; placeholder `app/{driver,dispatcher,admin}/page.tsx` with belt-and-suspenders role check + logout link (admin gets cross-tree nav). Root `app/page.tsx` becomes async server component that redirects signed-in users to their landing. BLOCKERS [supabase] amended re: `ld_session` replacement. E2E/integration tests explicitly skipped — unit coverage on the two load-bearing pure modules is the gate. All four automated gates green (typecheck/lint/test/build). Manual browser smoke skipped (no browser access in builder session). |
| l | seed-data | feature/seed-data | 956e874 | done | Demo-mode seed data for the mock layer — no new feature, just pre-fills in-memory storage so every role lands on populated screens under `pnpm dev` (USE_MOCKS=true). New: `mocks/seed.ts` exports `seedMocks()` / `isSeeded()` / `resetSeedFlag()`; seeds 6 offices (Chicago-area, office #4 phoneless, office #6 `active=false`), 10 doctors across the 5 active offices, 4 drivers (Miguel with `profileId: "user-driver"` to match the `driver@test` session + Alicia/Terrance/Casey — Terrance is `active=false`), 20 pickup requests hand-authored with backdated `createdAt` spread across the last 30 days (8 web / 6 sms / 4 email / 2 manual; 6 pending / 8 assigned / 4 completed / 2 flagged; 14 routine / 4 urgent / 2 stat), 2 routes for today (Route A = Miguel active with 5 stops — 2 completed + 1 on-site + 2 upcoming, `notified10min` true on stops 1-3; Route B = Alicia pending with 3 stops), 10 driver-location pings for Miguel spaced 90s apart, and 5 messages (3 linked to sms/email pickup requests + 2 orphans). New helpers on `mocks/storage.ts` (all test/seed-only, NOT part of StorageService): `seedPickupRequest`, `seedOffice`, `seedDoctor`, `seedDriver(driver, email)` — the last also populates the `driverAccounts` side map with `test1234` so the seeded driver can sign in. The seed flag lives at `globalThis.__labDispatchSeeded` so it survives Next.js HMR reloads without duplicating rows. `interfaces/getServices()` gains a private `maybeAutoSeed()` call in the mock branch gated on `process.env.NODE_ENV !== "test" && process.env.SEED_MOCKS !== "false" && !isSeeded()`; seed errors are caught and `console.warn`'d so a broken seeder never prevents boot. `resetAllMocks()` now also calls `resetSeedFlag()` so seed tests can re-seed after a reset (and the vitest global `beforeEach` keeps every test pristine). Tests: 11 new cases in `mocks/seed.test.ts` (counts across every table, idempotency, channel/status/urgency distributions, phoneless-office presence, soft-deleted row counts, Route A partial-check-in state, `driver@test` session binding to Miguel's route, 3-of-5 message-link count, isSeeded/resetSeedFlag toggle). 4 new cases in `interfaces/index.test.ts` (`NODE_ENV=test` suppresses auto-seed, `NODE_ENV=development` triggers it, `SEED_MOCKS=false` overrides dev, `resetAllMocks()` lets the next `getServices()` re-seed). Orchestrator decisions on the 3 plan open questions, all ACCEPT: (1) added `seedPickupRequest` test-only helper so requests can span 30 days; (2) added the 4th driver row keyed on `user-driver` so `/driver/route` shows stops when signed in as `driver@test`; (3) anchored the seed flag on `globalThis` for HMR safety. `mocks/README.md` gains a section documenting the auto-seed gate and the `SEED_MOCKS=false` escape hatch. All four gates green: typecheck 0 errors, lint 0 issues, vitest 451/451 (was 436), next build succeeds. |

## Integration adapters

One row per real (non-mock) adapter wired up behind an `interfaces/*` port. Every adapter ships with hermetic unit tests (no real HTTP) and is selected at runtime by `USE_MOCKS=false`.

| Adapter | Branch | Commit | Status | Notes |
|---------|--------|--------|--------|-------|
| adapter-supabase-storage | feature/adapter-supabase-storage | 541d47a | done | Real Supabase Postgres adapter for `StorageService`. New: `interfaces/supabase-client.ts` (`"server-only"` singleton admin client memoized on a `globalThis` Symbol; reads `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY`, throws `NotConfiguredError` naming the FIRST missing variable without leaking its value) + 5 tests. `interfaces/storage.real.ts` (`"server-only"`) with a per-method thin composition of supabase-js calls + mappers + error wrapping for every `StorageService` member except `createDriver`, which throws a scoped `"requires the Supabase auth adapter"` error today (plan Q1 accept — full `auth.admin.createUser` + `profiles` + `drivers` transaction lands in the auth adapter feature). `lib/supabase-mappers.ts` (pure row↔domain mappers for offices/drivers/doctors/pickup_requests/routes/stops/driver_locations/messages, plus `wrapSupabaseError(err, context)` that surfaces only PostgREST `code` + redacted `message` text — never URL, bearer/JWT, or the literal `service_role`; defense-in-depth regex redactions verified by tests) + 21 tests covering null→undefined, address split/reassemble, `DriverLocation.id` stringification, and four leak-proof assertions on the error wrapper. `tests/helpers/fake-supabase.ts` — in-memory PromiseLike query builder that records every chained call and drains per-`(table, op)` canned responses via `__enqueue`; exposes `auth.admin.listUsers` as a `vi.fn()` for the drivers join test + 5 sanity cases in its own test file. `interfaces/storage.real.test.ts` — 40+ per-method tests against the fake client with `vi.mock("@supabase/supabase-js")` at the module top: offices (8), drivers (6 including the `createDriver` scoped throw and `listDriverAccounts` auth-join), doctors (4), pickup requests (5 including `updatePickupRequestStatus` flaggedReason preservation/clearing), routes (3), stops (6 including `assignRequestToRoute` happy path + duplicate-assignment throw + `markStopNotified10min` idempotency), driver locations (2 with latest-per-driver dedupe), messages (5 including `createRequestFromMessage` + `linkMessageToRequest` idempotency), dashboards (3), and reorder/remove compound ops (3). `interfaces/storage.ts` drops its inline stub + `notConfigured()` helper and now re-exports `createRealStorageService` from the `"server-only"` module — callers' `import { StorageService } from "@/interfaces/storage"` still works. `interfaces/index.test.ts` gains one case asserting `services.storage.listOffices()` under `USE_MOCKS=false` with no Supabase env throws `NotConfiguredError` with `envVar="NEXT_PUBLIC_SUPABASE_URL"`. `vitest.setup.ts` adds one global `vi.mock("server-only", () => ({}))` (plan Q5 accept) so any file transitively importing the adapter loads cleanly under Node/Vitest; production/Next.js still gets the real `server-only` guard from the installed package. `package.json` adds `@supabase/supabase-js ^2.45.0` + the tiny `server-only` helper. `BLOCKERS.md` `[supabase]` entry updated to note the adapter is now half-wired (storage done; `createDriver` + auth pending). Accepted planner defaults on all 5 open questions: (1) `createDriver` scoped throw + real impl in the auth-adapter feature; (2) `listDriverAccounts` via `auth.admin.listUsers()` filtered by `drivers.profile_id`; (3) non-atomic multi-step ops (`assignRequestToRoute`, `removeStopFromRoute`, `reorderStops`, `createRequestFromMessage`) acceptable at v1 scale — UNIQUE constraints on `(route_id, position)` and `(route_id, pickup_request_id)` catch races; (4) `findOfficeByPhone` exact-match fast path + full-scan fallback with client-side `normalizeUsPhone` comparison; (5) `vi.mock("server-only")` global in `vitest.setup.ts`. Mock path intact — `USE_MOCKS=true` (default) continues to select mocks and every pre-existing test passes. All four gates green. |
| adapter-supabase-auth | feature/adapter-supabase-storage | _pending_ | done | Real Supabase-Auth-backed adapter for `AuthService` and simultaneous un-deferring of `createRealStorageService().createDriver`. New: `interfaces/auth.real.ts` (`"server-only"`) — `signIn` calls `supabase.auth.signInWithPassword`, then reads `profiles.role`+`full_name` via `maybeSingle`, returning `{ userId, role }`; every failure mode (auth error, null user, missing profile, unknown role, profile-read DB error) throws the single string `"invalid credentials"` so `app/login/actions.ts`'s generic "Invalid email or password." banner keeps working unchanged and no DB detail leaks to the user. `signOut` is best-effort — calls `supabase.auth.signOut()` on the admin client wrapped in try/catch (admin client holds no persistent user session; the user-visible logout still happens via `clearSession()` in `app/logout/route.ts`). `getCurrentUser` intentionally scoped-throws `"requires the cookie migration (STEP 4 in INTEGRATION_REPORT.md)"` — no consumer reaches it today (every resolver goes through `lib/session.ts::getSession()` reading `ld_session`). Defensive enum guard (plan Q5 accept): `ALLOWED_ROLES: readonly UserRole[] = ["driver","dispatcher","admin"]` rejects accidental widening from a future `profiles.role` enum migration. `interfaces/auth.ts` drops its inline `notConfigured()` stub and now re-exports `createRealAuthService` from the `"server-only"` module. `interfaces/auth.real.test.ts` — per-method hermetic coverage against the fake client with `vi.mock("@supabase/supabase-js")` at the top: `signIn` happy path × 3 roles (parametrized `it.each`), 5 failure modes (auth error, null user with no explicit error, missing profile, unknown role, profile-read DB error with defense-in-depth leak assertion), `signOut` (called exactly once, never throws when underlying rejects), `getCurrentUser` scoped STEP 4 error. `interfaces/storage.real.ts::createDriver` replaces its scoped throw with the full 3-step flow: `auth.admin.createUser` → `profiles` insert → `drivers` insert with joined profiles read-back + best-effort rollback (plan Q4 accept — non-transactional across auth+Postgres). Rollback semantics: createUser failure throws with no DB touch; profiles-insert failure rolls back the auth user only; drivers-insert failure rolls back profiles FIRST (drivers FK references profiles) THEN auth; if a rollback-delete itself fails, a `console.warn` is emitted with a context string (never the password, email, or raw error object) and the ORIGINAL error still surfaces. `TEMPORARY_DRIVER_PASSWORD = "test1234"` constant at the top of the file — shared temporary seed per plan Q3, flagged in BLOCKERS.md for replacement in a future driver-onboarding flow. `interfaces/storage.real.test.ts` — the old "createDriver throws scoped" case is replaced with 5 new tests: happy path, createUser failure (no DB calls, no rollback), profiles-insert failure (auth rollback only, drivers never touched), drivers-insert failure (profiles DELETE with `eq("id","u1")` + auth rollback), and rollback-auth-delete-itself-fails (original error still surfaces, `console.warn` spy verified). `tests/helpers/fake-supabase.ts` already had the extended auth surface (`auth.signInWithPassword`, `auth.signOut`, `auth.admin.createUser`, `auth.admin.deleteUser`) pre-wired during the storage adapter feature — no further changes needed; `fake-supabase.test.ts` already asserts every auth stub is a `vi.fn` and survives `__reset()`. `BLOCKERS.md` `[supabase]` entry updated to note interface-level adapters are complete and remaining work is the cookie rewire (STEP 4) before any real multi-user deployment; the shared test password + non-transactional rollback are flagged. Accepted planner defaults on all 5 open questions: (1) reuse admin client for `signInWithPassword` (one singleton; password validation is symmetric, service-role does not elevate privilege); (2) reference `INTEGRATION_REPORT.md` verbatim in the scoped `getCurrentUser` error (stable symbol to grep); (3) shared `"test1234"` temporary seed password flagged in BLOCKERS.md; (4) non-transactional rollback with `console.warn` logging on rollback-delete failure; (5) defensive `ALLOWED_ROLES` enum guard on `profiles.role`. Cookie mechanism (`lib/session.ts`, `app/login/actions.ts`, `app/logout/route.ts`, `middleware.ts`) and mock auth seeding are explicitly OUT OF SCOPE — STEP 4. Mock path intact — `USE_MOCKS=true` continues to select the three seeded mock accounts (`driver@test`, `dispatcher@test`, `admin@test`, shared password `"test1234"`). All four gates green. |
