# tests/

Integration and end-to-end tests that span multiple modules or exercise full request flows. Unit tests stay colocated next to the module under test (for example `lib/ids.test.ts` sits beside `lib/ids.ts`, and route tests live in colocated `__tests__/` directories under `app/`).
