# lib/

Pure TypeScript helpers, domain types, and concrete implementations of `interfaces/` ports (wrappers around external services like Supabase, Anthropic, Mapbox, SMS, and email). No React, no JSX, no direct DOM access; modules here are safe to import from both server and client unless a module is explicitly marked otherwise.
