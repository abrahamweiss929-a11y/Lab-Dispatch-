# supabase/

SQL migrations, row-level security policies, and seed scripts for the Supabase project. Application code that talks to Supabase (clients, query helpers, typed wrappers) lives in `lib/`, never here.
