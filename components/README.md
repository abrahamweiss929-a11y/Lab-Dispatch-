# components/

Shared presentational and composite React components used across routes. Route-specific UI stays colocated inside `app/`. Components here should accept their data via props and remain free of direct data fetching or business logic beyond what their props describe.
